# Seed e Operação

1. Crie um usuário manualmente em `Auth > Users` no painel Supabase.
2. Rode a migration em `supabase/migrations/202604170001_init_catalog_schema.sql`.
3. Faça login no app com o usuário criado.

Opcional: crie um catálogo inicial para teste via interface `/upload`.
